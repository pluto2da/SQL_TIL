# 📘 SQL_BASIC 3주차 정규 과제

SQL_BASIC 정규 과제는 매주 정해진 분량의 `초보자를 위한 BigQuery(SQL) 입문` 강의를 듣고, 핵심 개념을 정리한 뒤 간단한 SQL 문제를 직접 풀어보는 방식으로 진행합니다.

이번 주는 집계 함수와 `GROUP BY`, `HAVING`을 학습합니다.

완성된 과제는 Github에 업로드하고, 링크를 스프레드시트 'SQL' 시트에 입력해 제출해주세요.

**👀 수행 인증란은 필수입니다.**

---

## 📚 SQL_BASIC_3rd_TIL

### 섹션 3. 데이터 탐색 - 조건, 추출, 요약

### 2-5. 집계(GROUP BY + HAVING + SUM/COUNT)

### 2-7. 정리

### 2-8. 새로운 집계 함수 소개(GROUP BY ALL, 2024-02-26에 나온 함수)

---

## ✨ 선택 강의

- 2-6. 연습 문제: 집계와 조건 조회를 더 연습하고 싶을 때 선택 수강

---

## 🏁 전체 강의 수강 계획

| 주차 | 필수 강의 범위 | 선택 강의 | 완료 여부 |
| --- | --- | --- | --- |
| 1주차 | 1-1 ~ 2-1 | 1 | ✅ |
| 2주차 | 2-2 ~ 2-3 | 2-4 | ✅ |
| 3주차 | 2-5, 2-7 ~ 2-8 | 2-6 | ✅ |
| 4주차 | 3-2 ~ 4-3 | 3-4 | 🍽️ |
| 5주차 | 4-4 ~ 4-6 | 4-5, 4-7 | 🍽️ |
| 6주차 | 5-2 ~ 5-5 | 5-6 | 🍽️ |
| 7주차 | 필수 강의 없음 | 6-2, 6-3, 6-4, 6-5 | 🍽️ |

---

<br>

<!-- 여기까진 그대로 둬 주세요-->

---

# 1️⃣ 개념 정리

아래 키워드 중 중요하다고 생각한 개념을 2개 이상 골라 짧게 정리해주세요. 3개보다 더 많이 정리하고 싶다면 자유롭게 항목을 추가해도 좋습니다.

이번 주 키워드:
- COUNT
- SUM
- AVG
- MAX
- MIN
- GROUP BY
- HAVING
- 집계 기준

## 01.

```
개념 이름: AVG
개념 설명: AVG는 집계 함수 중 한 가지로 숫자형 데이터의 평균값을 계산할 때 활용한다. 데이터 분석시 결측치는 평균 계산에서 제외된다는 점을 주의해야 정확한 통계 수치를 얻을 수 있다.
예시 쿼리: 대략 이런 쿼리를 작성할 수 있다.
  SELECT
    AVG(PRICE) AS 평균 판매가
  FROM
    PRODUCT
```

## 02.

```
개념 이름: SUM
개념 설명: SUM은 총합을 구하는 함수로 지정된 컬럼의 모든 값을 더해준다. 매출액이나 방문자의 총 수처럼 데이터의 전체 규모를 파악할 때 기본적인 함수로 활용하게 된다.
예시 쿼리:
  SELECT
    SUM(PRICE) AS 총_판매가
  FROM
    PRODUCT;
```

## (선택) 03.

```
개념 이름: 집계 기준
개념 설명: 집계 기준이라는건 데이터를 어떤 단위로 묶어서 요약할지 결정하는 기준 컬럼을 의미한다. 방대한 데이터를 동물 종별로 혹은 날짜별 등 어떤 관점으로 쪼개서 분석할 것인지를 결정하는 척도가 되는 것이다.
헷갈린 점: 처음에는 SELECT 절에 나열한 컬럼 중에서 MAX와 COUNT 같은 집계 함수가 씌워지지 않은 일반 컬럼들은 빠짐없이 모두 GROUP BY 절에 하나의 집계 기준으로 적어줘야 하는 규칙이 번거롭게 느껴졌다. 하지만 강의에 추가되어 있는 GROUP BY ALL 구문을 써보며 편리함을 체감했기 때문에 또 기준 컬럼을 어떻게 분리하고 지정해야 하는지 그 원리를 더 확실하게 이해할 수 있었던 것 같다.
```

---

# 2️⃣ 수행 인증란

아래 중 하나 이상을 첨부해주세요.

- 강의 수강 화면 캡처
- 문제 풀이 정답 화면 캡처
- SQL 실행 결과 화면 캡처

<img width="1228" height="950" alt="image" src="https://github.com/user-attachments/assets/0be406e1-a986-4b6d-b956-13b4c19a252e" />

<img width="1300" height="1058" alt="image" src="https://github.com/user-attachments/assets/7a383d20-1553-4bd2-b729-41cc2a9dde95" />


---

# 3️⃣ 확인 문제

프로그래머스는 로그인이 필요하므로, 로그인 후 문제 풀이를 진행해주세요.

## 🧩 문제 1

문제 링크: [최댓값 구하기](https://school.programmers.co.kr/learn/courses/30/lessons/59415)

풀이 과정: 
    SELECT
      MAX(DATETIME) AS 시간
    FROM
      ANIMAL_INS

```
- 문제 요구사항: ANIMAL_INS 테이블에 기록된 데이터에서 가장 최근에 들어온 동물의 보호 시작일(DATETIME)을 찾고 최종 출력되는 결과의 컬럼명을 시간으로 지정하는 것
- 사용한 SQL 절: 두 가지 쿼리를 작성함.

  1) SELECT, FROM 절과 함께 MAX() 집계 함수를 사용했고, 날짜나 시간 데이터에 MAX()를 적용하면 크기가 가장 큰 값을 출력하기에 미래에 가장 가까운 최신 시점을 정확히 반환해주는 방법이다.
  2) ORDER BY DATETIME DESC 절을 사용하여 최신 날짜부터 내림차순으로 전체 데이터를 정렬하고, LIMIT 1 절로 맨 위에 위치한 첫 번째 행만 추출하는 방법도 있다.
    SELECT
      DATETIME AS 시간
    FROM
      ANIMAL_INS
    ORDER BY
      DATETIME DESC
    LIMIT 1
    이런 형태의 코드가 된다.

- 새로 배운 점:

  숫자뿐만 아니라 날짜와 시간(DATETIME) 데이터 형식에도 크기를 비교하는 집계 함수인 MAX, MIN와 ORDER BY가 동일하게 적용된다는 점이 새롭게 느껴졌다. 이번 문제처럼 최신 시간과 같은 단일 값만 구할 때는 MAX()가 효율적이라고 느껴졌지만, 만약 '가장 최근에 들어온 동물의 이름과 시간'과 같은 여러 조건을 동시에 요구한다면 다른 컬럼의 데이터를 함께 유지할 수 있는 ORDER BY & LIMIT 조합의 풀이가 훨씬 유용하게 쓰인다는 사실을 기억하면 좋을 것 같다.
```

<img width="2190" height="1406" alt="image" src="https://github.com/user-attachments/assets/51cb0f50-3435-4be5-8067-dd7b3a134247" />


## 🧩 문제 2

문제 링크: [가장 비싼 상품 구하기](https://school.programmers.co.kr/learn/courses/30/lessons/131697)

풀이 과정: 
  SELECT
    MAX(PRICE) AS MAX_PRICE
  FROM 
    PRODUCT

```
- 사용한 집계 함수: MAX()
  -> 괄호 안에 PRICE 컬럼을 지정해서 값 중 가장 큰 최댓값을 반환하도록 한다.
- 집계 대상 컬럼: PRICE
- 결과를 검증한 방법:
  1) 예시 데이터를 보면 PRICE 값이 10000, 9000, 22000으로 주어져 있는데, 이 중 가장 높은 값은 22000이기에 작성한 쿼리 실행 결과가 MAX_PRICE 컬럼명에 22000으로 도출되는지 확인하면 검증이 가능하다.
  2) ORDER BY를 활용해서 검증할 수도 있다.
    SELECT
      PRICE
    FROM
      PRODUCT
    ORDER BY
      PRICE DESC
    이렇게 쿼리를 실행하여 가격을 내림차순으로 정렬했을때, 맨 위에 첫 번째 값으로 MAX() 함수로 구한 값과 동일하게 나오는지 확인하여 검증하면 된다.
```

<img width="2182" height="1482" alt="image" src="https://github.com/user-attachments/assets/4082707f-f2f2-48e3-8614-8384ddf640b8" />


## 🧩 문제 3

문제 링크: [고양이와 개는 몇 마리 있을까](https://school.programmers.co.kr/learn/courses/30/lessons/59040)

풀이 과정:
  SELECT 
    ANIMAL_TYPE, COUNT(ANIMAL_ID) AS count
  FROM 
    ANIMAL_INS
  WHERE
    ANIMAL_TYPE IN ('Cat', 'Dog')
  GROUP BY
    ANIMAL_TYPE
  ORDER BY
    ANIMAL_TYPE ASC

```
- 그룹화 기준: 동물 보호소에 들어온 동물을 종별로 각각 나누어 마리 수를 계산하라고 했기 때문에 그룹화 기준은 ANIMAL_TYPE.

- WHERE와 HAVING 중 사용한 절: WHERE절 사용.
  고양이와 개 데이터만 필터링하기 위해 WHERE ANIMAL_TYPE IN ('Cat', 'Dog')를 작성하는 것이 맞고, HAVING은 예를 들어 종류별로 몇 마리 이상인 종만 출력하라는 느낌의, 그룹화가 모두 끝난 후 집계된 결과 값에 조건을 걸 때 사용하기에 이번엔 사용하지 않았다.

- 처음 틀렸다면 틀린 이유: 처음에 틀리진 않았는데 주의할 부분은 있었다. 문제에서 고양이를 개보다 먼저 조회해달라고 했기 때문에 ORDER BY에서 알파벳 순서에 맞게 오름차순 정렬을 해줘야 했다. 

- 새로 배운 SQL 패턴: WHERE 절로 분석할 대상만 먼저 골라내고, GROUP BY를 활용해 특정 범주로 묶은 다음에, COUNT 집계 함수 적용하고 ORDER BY로 원하는 순서대로 정렬하는 구조를 잘 숙지해야 한다.
```

<img width="2420" height="1426" alt="image" src="https://github.com/user-attachments/assets/410fac12-5968-45d2-9c33-676342d1a668" />


---

# 4️⃣ 이번 주 회고

```
1. 문제를 SQL로 옮길 때 가장 어려웠던 부분:
  문제를 SQL 쿼리로 변환하는 논리적 흐름 자체에서 크게 막히는 부분은 없었지만 이번주 강의를 들으면서 GROUP BY ALL의 유용성에 주목할 수 있었다. 집계 함수를 사용할 때 원래는 first_name, last_name 등 그룹화할 기준 컬럼들을 일일이 명시해야 해서 번거로움이 있었는데 추가된 GROUP BY ALL 구문을 적용했을때 기준 컬럼을 하나하나 수동으로 분리해 지정할 필요 없이 자동으로 묶어줘서 쿼리 작성이 훨씬 효율적이고 직관적이라는 점이 좋게 느껴졌다.

2. WHERE와 HAVING의 차이를 어떻게 이해했는지:
  WHERE과 HAVING은 데이터를 필터링하는 순서와 시점의 차이로 정리할 수 있다고 생각한다.
  WHERE 절은 원본 데이터에서 분석에 필요한 대상만 1차적으로 골라낼 때 사용할 수 있고,
  HAVING 절은 GROUP BY를 통해 데이터를 특정 범주로 묶어서 집계를 완료한 결과값에 2차적 조건을 걸 때 사용한다는 점에서 그 차이가 있다고 이해했다.

3. 다음 주에 더 연습하고 싶은 문제 유형: 이번 강의를 통해 그룹화와 집계 함수를 다루는 기초를 다졌으니 다음 주에는 다중 테이블을 연결하는 JOIN 연산이나 데이터 내에서 순위나 누적된 합을 구하는 문제들을 집중적으로 연습해보면 좋을 것 같다.

```

수고하셨습니다!




