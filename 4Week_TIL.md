# 📘 SQL_BASIC 4주차 정규 과제

SQL_BASIC 정규 과제는 매주 정해진 분량의 `초보자를 위한 BigQuery(SQL) 입문` 강의를 듣고, 핵심 개념을 정리한 뒤 간단한 SQL 문제를 직접 풀어보는 방식으로 진행합니다.

이번 주는 SQL 쿼리를 작성하는 흐름, 쿼리 작성 템플릿, 데이터 타입 변환, 문자열 함수를 학습합니다.

완성된 과제는 Github에 업로드하고, 링크를 스프레드시트 'SQL' 시트에 입력해 제출해주세요.

**👀 수행 인증란은 필수입니다.**

---

## 📚 SQL_BASIC_4th_TIL

### 섹션 4. SQL 쿼리 잘 작성하기, 쿼리 작성 템플릿 및 오류를 잘 디버깅하기

### 3-2. SQL 쿼리를 작성하는 흐름

### 3-3. 쿼리 작성 템플릿과 생산성 도구

### 섹션 5. 데이터 탐색 - 변환

### 4-1. INTRO

### 4-2. 데이터 타입과 데이터 변환(CAST, SAFE_CAST)

### 4-3. 문자열 함수(CONCAT, SPLIT, REPLACE, TRIM, UPPER)

---

## ✨ 선택 강의

- 3-4. 오류를 디버깅하는 방법: 오류 메시지 해석과 디버깅 흐름을 더 익히고 싶을 때 선택 수강

---

## 🏁 전체 강의 수강 계획

| 주차 | 필수 강의 범위 | 선택 강의 | 완료 여부 |
| --- | --- | --- | --- |
| 1주차 | 1-1 ~ 2-1 | 1 | ✅ |
| 2주차 | 2-2 ~ 2-3 | 2-4 | ✅ |
| 3주차 | 2-5, 2-7 ~ 2-8 | 2-6 | ✅ |
| 4주차 | 3-2 ~ 4-3 | 3-4 | ✅ |
| 5주차 | 4-4 ~ 4-6 | 4-5, 4-7 | 🍽️ |
| 6주차 | 5-2 ~ 5-5 | 5-6 | 🍽️ |
| 7주차 | 필수 강의 없음 | 6-2, 6-3, 6-4, 6-5 | 🍽️ |

---

<br>

<!-- 여기까진 그대로 둬 주세요-->

---

# 1️⃣ 개념 정리

아래 키워드 중 중요하다고 생각한 개념을 2개 이상 골라 짧게 정리해주세요. 3개보다 더 많이 정리하고 싶다면 자유롭게 항목을 추가해도 좋습니다.

이번 주 키워드:
- 쿼리 작성 순서
- 쿼리 작성 템플릿
- 데이터 타입
- CAST
- SAFE_CAST
- CONCAT
- REPLACE
- TRIM

## 01.

```
개념 이름: CONCAT
개념 설명:
  CONCAT은 둘 이상의 흩어져 있는 문자열이나 컬럼 값을 하나로 매끄럽게 이어 붙여주는 함수를 말한다.
  주소 데이터의 시/도와 상세 주소를 합치거나 코드 앞뒤에 특정 단어를 덧붙여 텍스트를 보기 좋게 가공할 때 유용하게 쓸 수 있다.
예시 쿼리:
  SELECT
    CONCAT(FACTORY_NAME, ' - ', ADDRESS) AS 공장_정보
  FROM
    FOOD_FACTORY
```

## 02.

```
개념 이름: REPLACE
개념 설명:
  REPLACE는 영단어 뜻 그대로 문자열 안에서 찾고자 하는 특정 단어나 패턴을 찾아서 다른 문자열로 바꿔주는 함수이다.
  데이터 전처리 과정에서 콤마, 괄호, 오탈자 같은 불필요한 기호를 제거하거나 다른 표현으로 대체할 때 자주 쓰인다.
예시 쿼리:
  SELECT
    REPLACE(OPTIONS, '열선시트', '시트열선') AS 옵션_수정
  FROM
    CAR_RENTAL_COMPANY_CAR
```

## (선택) 03.

```
개념 이름: SAFE_CAST
개념 설명:
  가장 낯선 개념이기도 했는데, SAFE_CAST는 데이터의 형식을 다른 타입으로 안전하게 변환해 주는 함수를 말한다.
  일반 변환 함수와 달리 숫자에 문자가 섞여 있는 등 변환할 수 없는 이상한 값이 들어와도 쿼리 전체가 에러가 나는 것이 아니라,
  변환 실패 시 NULL 값을 반환하며 유연하게 넘어가도록 해주는 역할이다.
헷갈린 점:
  그냥 CAST 함수와의 차이점이 처음엔 무엇인지 헷갈리게 다가왔던 것 같다. SAFE_CAST를 활용하면 문자가 섞여 있는 컬럼이라도 쿼리가 에러 나지 않고 돌아간다는 것을 알고 예외 데이터만 깔끔하게 NULL로 걸러진다는 장점을 이해하고 나니 그 용도와 유용성이 명확하게 정리된 것 같다.
```

---

# 2️⃣ 수행 인증란

아래 중 하나 이상을 첨부해주세요.

- 강의 수강 화면 캡처
- 문제 풀이 정답 화면 캡처
- SQL 실행 결과 화면 캡처

---

# 3️⃣ 확인 문제

프로그래머스는 로그인이 필요하므로, 로그인 후 문제 풀이를 진행해주세요.

## 🧩 문제 1

문제 링크: [특정 옵션이 포함된 자동차 리스트 구하기](https://school.programmers.co.kr/learn/courses/30/lessons/157343)

풀이 과정: 
```
  SELECT 
    CAR_ID, CAR_TYPE, DAILY_FEE, OPTIONS
  FROM 
    CAR_RENTAL_COMPANY_CAR
  WHERE
    OPTIONS LIKE '%네비게이션%'
  ORDER BY
    CAR_ID DESC
```

```
- 찾으려는 문자열 조건: OPTIONS 컬럼 내에 '네비게이션'이라는 키워드가 포함되어 있는지 여부를 찾고자 했다.
- 사용한 문자열 조건 문법:
  LIKE '%네비게이션%' 문법을 사용했다. %를 앞뒤로 붙여 '네비게이션' 앞이나 뒤에 어떤 다른 옵션 문자열이 오더라도 해당 단어가 포함되어 있다면 모두 조회되도록 작성하였다.
- 정렬 기준: 자동차 ID(CAR_ID) 컬럼을 기준으로 한 내림차순 정렬 기준이다. 문제 요구사항에 따라 번호가 큰 차량부터 먼저 출력되도록 설정한다.
```

<img width="2256" height="1494" alt="image" src="https://github.com/user-attachments/assets/6967ab42-0ddb-4efe-b2ce-442b9bc54d8c" />


## 🧩 문제 2

문제 링크: [강원도에 위치한 생산공장 목록 출력하기](https://school.programmers.co.kr/learn/courses/30/lessons/131112)

풀이 과정:
```
  SELECT 
    FACTORY_ID, FACTORY_NAME, ADDRESS
  FROM
    FOOD_FACTORY
  WHERE
    ADDRESS LIKE '강원도%'
  ORDER BY
    FACTORY_ID ASC
```

```
- 문제에서 요구한 조건:
  FOOD_FACTORY 테이블에서 주소가 '강원도'에 위치한 식품공장을 찾아 공장 ID, 공장 이름, 주소 컬럼만을 조회하고 출력 순서는 공장 ID 기준으로 오름차순 정렬하는 조건이었다. 전화번호 컬럼은 출력 대상에서 제외한다.
- WHERE 절로 옮긴 방식:
  ADDRESS 컬럼의 데이터 타입은 가변 길이 문자열이고 주소 데이터 특성상 '강원도 정선군...', '강원도 원주시...'와 같이 지역명이 문자열의 맨 앞에 위치하기 때문에 LIKE 연산자와 함께 뒤에 오는 모든 문자열을 대체하는 %를 '강원도' 뒤에 붙여 '강원도로 시작하는 모든 행'을 필터링하도록 구현한다. 이때 주소 체계상 시작 지점을 명시하기 위해 앞에는 %를 붙이지 않았다.
- 정렬 기준:
  ORDER BY FACTORY_ID ASC를 사용하여 문자열 및 숫자 조합 기준 오름차순(사전순)으로 가장 빠른 값부터 차례대로 출력하도록 설정한다. 오름차순이 기본값이라 ASC는 생략해도 괜찮다.
```

<img width="2056" height="1438" alt="image" src="https://github.com/user-attachments/assets/734618e5-22bc-4563-a9ee-5d312dac39f7" />


## 🧩 문제 3

문제 링크: [이름에 el이 들어가는 동물 찾기](https://school.programmers.co.kr/learn/courses/30/lessons/59047)

풀이 과정:
```
  SELECT
    ANIMAL_ID, NAME
  FROM
    ANIMAL_INS
  WHERE
    ANIMAL_TYPE = 'Dog'
    AND LOWER(NAME) LIKE '%el%'
  ORDER BY
    NAME ASC, ANIMAL_ID ASC
```

```
- 찾으려는 문자열 패턴:
  이름 문자열 내에 'el' 또는 'EL'이라는 알파벳 조합이 위치와 무관하게 포함되어 있는지 확인해야 한다. 앞뒤에 어떤 글자가 오더라도 매칭되도록 %를 결합한 %el% 형태를 사용했고 동물 종이 개인 조건도 함께 충족하도록 구성했다.
- 대소문자를 처리한 방식:
  표준 SQL 환경과 대소문자를 엄격히 구분하는 DBMS에서도 일관되게 동작하도록 LOWER(NAME) 함수를 적용하여 컬럼의 문자열을 모두 소문자로 변환한 뒤 소문자 패턴인 '%el%'과 비교하는 방식을 사용했다.
- 정렬 기준: 1차적으로 이름(NAME)을 기준으로 오름차순 정렬하고, 이름이 중복되는 경우를 대비해 2차적으로 동물 아이디를 기준으로 오름차순 정렬하는 다중 정렬 조건을 지정했다.
```

<img width="2096" height="1520" alt="image" src="https://github.com/user-attachments/assets/d111d9db-accd-4916-b137-98070cbc661a" />


## 🧩 문제 4

문제 링크: [카테고리 별 상품 개수 구하기](https://school.programmers.co.kr/learn/courses/30/lessons/131529)

풀이 과정:
```
  SELECT 
    SUBSTRING(PRODUCT_CODE, 1, 2) AS CATEGORY, COUNT(PRODUCT_ID) AS PRODUCTS
  FROM
    PRODUCT
  GROUP BY 
    SUBSTRING(PRODUCT_CODE, 1, 2)
  ORDER BY
    CATEGORY ASC
```

```
- 추출한 문자열 범위:
  PRODUCT_CODE 컬럼의 첫 번째 글자부터 시작하여 2개의 글자까지를 추출하는데 SUBSTRING(PRODUCT_CODE, 1, 2) 함수를 활용했고, 8자리 상품코드 중 앞 2자리 카테고리 코드만을 잘라낼 수 있게된다.
- 그룹화 기준:
  추출한 앞 2자리 카테고리 코드를 기준으로 그룹화해야 한다. 잘라낸 카테고리 단위별로 데이터를 묶은 다음에 COUNT(PRODUCT_ID) 집계 함수를 적용하여 해당 카테고리에 속한 상품의 총 개수를 계산한다.
- 정렬 기준:
  카테고리 코드 컬럼을 기준으로 오름차순 정렬하면 된다. 알파벳 순서상 빠른 카테고리 코드부터 차례대로 출력된다.
```

<img width="2752" height="1464" alt="image" src="https://github.com/user-attachments/assets/16c8d7bf-f4d4-457e-b576-8c304e34e34a" />


---

# 4️⃣ 이번 주 회고

```
1. 쿼리 작성 흐름을 잡을 때 도움이 된 방법:
  문제 지문을 읽고 나면 프로그래머스 내에 SELECT 절이 작성되어 있어 그냥 바로 작성을 시작하기 바쁠때가 많았는데, 처리 순서에 맞춰 대상 테이블 지정(FROM)을 먼저 하고, 필요한 행을 필터링(WHERE)한 다음에 그룹화 및 연산(GROUP BY/집계), 마지막에 정렬(ORDER BY) 흐름으로 단계를 나눠서 생각하니까 헷갈리지 않는 것 같다는 생각이 들었다. 특히 이번주에 배운 LIKE나 문자열 추출 함수를 적용할 때는 전체 데이터 중에서 어떤 조건의 행을 먼저 남길지 WHERE 절 조건부터 먼저 구체화해서 정해두고 들어가니까 쿼리가 더 명확하게 완성된 것 같다.
2. 타입 변환이나 문자열 처리에서 조심해야 할 점:
  문자열을 다룰 때 LIKE와 같은 부분 일치 상황에서 %의 위치와 범위를 정교하게 지정해야 한다는 것을 주의해야 한다. 조건에 따라서는 대소문자도 구분할 수 있기 때문에  LOWER()나 UPPER()도 적절히 활용하는 것을 연습해야 한다. SUBSTRING처럼 문자열의 일부를 잘라내어 기준을 삼을 때는 시작 인덱스와 추출할 길이를 정확히 확인하지 않으면 의도치 않은 그룹으로 묶일 수 있어 주의해야 한다는 점도 기억할 필요가 있다.
3. 앞으로 문제 풀이 때 먼저 확인할 것:
  우선 데이터 타입(VARCHAR, INTEGER, DATETIME 등)에 따라 적용할 수 있는 함수나 연산자가 달라지고, 결측치 처리 방식도 달라지기 때문에 각 컬럼의 데이터 타입과 NULL 허용 여부를 가장 먼저 확인하도록 할 예정이다. 또한 문제가 길어지면서 대소문자나 정렬의 세부 기준 등 문제의 조건도 다양해지기 때문에 이를 놓치지 않도록 요구사항을 하나씩 대조하며 푸는 습관을 들여야 할 것 같다.
```

수고하셨습니다!




